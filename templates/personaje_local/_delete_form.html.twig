<form method="post" action="{{ path('app_personaje_local_delete', {'id': personaje.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personaje.id) }}">
    <button class="btn">Delete</button>
</form>
